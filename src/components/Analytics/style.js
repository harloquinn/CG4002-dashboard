import styled from 'styled-components'
import { BUTTON_COLOR, BG_COLOR } from '../constants';

const AnalyticsStyles = styled.div`
 .accelerometer-graph {
   width: 600px;
   height: 400px;
 }
 .gyroscope-graph {
  width: 600px;
  height: 400px;
}
`;

export default AnalyticsStyles;